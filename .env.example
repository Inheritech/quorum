# Copy to .env.local after creating a Convex deployment.
NEXT_PUBLIC_CONVEX_URL=
# Local Convex CLI configuration (set by pnpm convex:dev).
CONVEX_DEPLOYMENT=
# Vercel only: store CONVEX_DEPLOY_KEY as a sensitive build variable.
# Never prefix the deploy key with NEXT_PUBLIC_.
